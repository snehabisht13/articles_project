<h1>Bloggers</h1>

<% @users.each do |user|%>
    <div>
    <h2>Name: <%= link_to user.username, user_path(user) %></h2>
    <p> Email: <%= user.email%></p>
    <%= link_to 'View Articles', user_path(user)%>
   </div>
    <%end%>